[package]
name = "anvil-documentation"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
name = "anvil_documentation"

[dependencies]
anyhow = "1"
fission = { version = "0.5.0", default-features = false, features = ["site"] }
