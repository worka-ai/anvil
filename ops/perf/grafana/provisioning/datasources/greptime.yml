apiVersion: 1

datasources:
  - name: GreptimeDB
    uid: greptime
    type: mysql
    access: proxy
    url: greptime:4002
    database: public
    user: root
    jsonData:
      timezone: UTC
      maxOpenConns: 10
      maxIdleConns: 5
      connMaxLifetime: 14400
    isDefault: true
